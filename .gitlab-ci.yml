image: shr3jn/android-fastlane:jdk11

stages:
  - build

before_script:
  - export GRADLE_USER_HOME=/gradle
  - chmod +x ./gradlew

cache:
  key: ${CI_PROJECT_ID}
  paths:
    - .gradle

build:
  stage: build
  script:
    - ./gradlew assembleDebug